x-env: &env
  KAFKA_HOST: kafka
  REDIS_HOST: redis
  MINIO_HOST: minio

x-common: &common
  env_file:
    - .env
  networks:
    - localnet

services:
  auth:
    build:
      context: .
    ports:
      - 3020:3020 # Rest
    environment:
      SERVICE_NAME: auth
      <<: *env
    <<: *common

networks:
  localnet:
    driver: bridge
