// @ts-ignore: Ensure this file is treated as a module and recognized by TS project service
import DefaultTheme from 'vitepress/theme';
// @ts-ignore: CSS import may not be recognized by the TS project service
import './custom.css';

// Export the default theme plainly so the file is a proper module
export default DefaultTheme;
