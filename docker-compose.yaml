version: '3.8'

x-common: &common
  build:
    context: .
  env_file:
    - .env
  environment:
    DEBUG: '*'
    NODE_ENV: prod
  networks:
    - platform

services:
  gateway:
    build:
      args:
        APP_NAME: gateway
    ports:
      - '3001:3001' # Rest
      - '5001:5001' # gRPC
    environment:
      SENTRY_DSN:
    <<: *common

networks:
  platform:
    driver: bridge
